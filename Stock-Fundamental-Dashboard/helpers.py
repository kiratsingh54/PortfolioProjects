def format_number(number):  #formats the number for website 
    return f"{number:,}" 

def format_long_number(number): #formats the number for website and limits the degits after the decimal point
    return f"{number:2f}" 